---
trivial:
  - "docs - make `CLAUDE.md` an index. It holds the rules of the repository
    and links to the document of each part, and it no longer repeats what a
    document says. The description of each workflow and each job moves to the
    new `docs/design/checks.md`, which corrects four statements that the copy
    had drifted into. The changelog format, the check commands and the names
    of the required jobs each now live in one file
    (https://github.com/maglo/qemu-lab-manager/issues/24)."
